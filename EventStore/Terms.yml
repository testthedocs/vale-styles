---
extends: substitution
message: Use '%s' instead of '%s'
level: error
ignorecase: true
swap:
  eventstore: Event Store
  the event store: Event Store
  web ui: Admin UI
  web admin ui: Admin UI
  instance: node
